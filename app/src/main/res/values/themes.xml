<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; Compose drives the real colors. This keeps a splash-safe
         Material 3 parent so the app has a valid theme before Compose starts. -->
    <style name="Theme.OnDeviceLLM" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
